/**
 * Testes legados de um deckParser local (não existe mais no frontend).
 * A análise canônica vive no backend (`/api/analyzeDeck`).
 *
 * Mantido como placeholder (skip) para não quebrar `npm test`.
 */

test.skip("legacy: Detecta arquétipo e tier", () => {});

test.skip("legacy: Heatmap é gerado", () => {});
