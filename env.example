# ═══════════════════════════════════════════════════════════════
# LORCANA AI - BACKEND ENVIRONMENT VARIABLES
# ═══════════════════════════════════════════════════════════════

# ── Server ─────────────────────────────────────────────────────
PORT=3002
NODE_ENV=development

# ── Supabase ───────────────────────────────────────────────────
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_ANON_KEY=sua-anon-key-aqui
SUPABASE_SERVICE_KEY=sua-service-key-aqui

# ── Authentication ─────────────────────────────────────────────
# JWT Secret (mínimo 32 caracteres) - Gerar novo em produção
JWT_SECRET=sua-secret-key-minimo-32-caracteres-aqui
JWT_EXPIRES_IN=7d

# Encryption Key (64 caracteres hex) - Gerar novo em produção
# Gerar com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=sua-encryption-key-64-caracteres-hex-aqui

# ── Email Service ──────────────────────────────────────────────
# OPÇÃO 1: Gmail (Desenvolvimento)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu.email@gmail.com
SMTP_PASS=sua-senha-app-aqui

# OPÇÃO 2: SendGrid (Produção - comentar Gmail e descomentar)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=SG.sua-api-key-aqui

# OPÇÃO 3: Outro SMTP
# SMTP_HOST=smtp.seudominio.com
# SMTP_PORT=587
# SMTP_USER=noreply@seudominio.com
# SMTP_PASS=sua-senha

# ── Application URLs ───────────────────────────────────────────
# Frontend URL (para links em emails)
APP_URL=http://localhost:3000

# Backend URL (para API)
API_URL=http://localhost:3002

# ═══════════════════════════════════════════════════════════════
# INSTRUÇÕES
# ═══════════════════════════════════════════════════════════════

# 1. Copie este arquivo para .env
#    cp .env.example .env

# 2. Configure o Supabase
#    - Criar projeto em supabase.com
#    - Copiar URL e keys de Settings > API

# 3. Gerar chaves de segurança
#    - JWT_SECRET:
#      node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#    
#    - ENCRYPTION_KEY:
#      node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# 4. Configurar Email (Gmail)
#    a) Ativar verificação em 2 etapas: https://myaccount.google.com/security
#    b) Gerar senha de app: https://myaccount.google.com/apppasswords
#    c) Copiar senha gerada (16 caracteres) para SMTP_PASS

# 5. Testar email
#    node test-email.js

# 6. Para produção:
#    - Mudar NODE_ENV=production
#    - Usar SendGrid ou outro serviço profissional
#    - Atualizar APP_URL e API_URL para domínios reais
#    - NUNCA commitar .env no git!
